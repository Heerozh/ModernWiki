# ModernWiki Environment Configuration

# Git repository containing your Hugo/Markdown content
GIT_REPO=https://github.com/Heerozh/ModernWikiTemplate.git

# Git branch to use (default: main)
GIT_BRANCH=master

# Domain name for reverse proxy (e.g., mywiki.com), for debug use :80
LISTEN_DOMAIN=:80
# Base URL for Hugo site, use for redirects (e.g., https://mywiki.com/)
HUGO_BASEURL=http://127.0.0.1/

# Webhook secret for signature verification, support GitHub, Gitea, GitLab
# Generate a secure random string for this value
# You can use: python -c "import secrets; print(secrets.token_hex(32))"
WEBHOOK_SECRET=

# Gitea configuration
GITEA____APP_NAME="ModernWiki Git"
# You must expand this to HUGO_BASEURL + /git/
GITEA__SERVER__ROOT_URL=http://127.0.0.1/git/


# Enable Artalk comments?
HUGO_PARAMS_COMMENTS_ENABLE=true
# Artalk config
TZ=Asia/Shanghai
# Domain name for reverse proxy (e.g., comments.mywiki.com), for debug use :8080
ATK_LISTEN_DOMAIN=:8080
# use subdomain for Artalk, e.g., https://comments.mywiki.com, for debug use http://127.0.0.1:8080/
ATK_SITE_URL=http://127.0.0.1:8080/
ATK_LOCALE=zh-CN
ATK_FRONTEND_DARKMODE=auto
# Comment site database name, do not change，不可修改
ATK_SITE_DEFAULT=ModernWiki   
# Login enable?
ATK_AUTH_ENABLED=false
# Enable email login?
ATK_AUTH_EMAIL_ENABLED=true
# Gitea OAuth2 configuration 
# ATK_AUTH_GITEA_CLIENT_ID=
# ATK_AUTH_GITEA_CLIENT_SECRET=
# ATK_AUTH_GITEA_URL=http://127.0.0.1/git/  目前artalk不支持自定义Gitea地址，等以后
# ATK_AUTH_GITEA_ENABLED=true 
# ATK_AUTH_STEAM_API_KEY=
# ATK_AUTH_STEAM_ENABLED=true
